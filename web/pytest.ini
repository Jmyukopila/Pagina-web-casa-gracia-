[pytest]
asyncio_mode = auto
testpaths = tests
# Put the app package (web/) on sys.path so `import app` works regardless of how
# pytest is launched (`pytest` console script vs `python -m pytest`). Without
# this, CI's `pytest -q` can't import `app` (no CWD on sys.path).
pythonpath = .
